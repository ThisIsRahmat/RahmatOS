---

import Layout from '../../layouts/Layout.astro';



export async function getStaticPaths() {
  const titles = [
    "book-title",
    "second-book-title", // Corrected typo and lowercase
    "third-book-title",
  ]; // Added comma at the end

  return titles.map((title) => ({
    params: { title },
  }));
}
---


<Layout title="Books">


</Layout>